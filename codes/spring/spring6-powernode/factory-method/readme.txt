工厂方法模式：Factory Method Pattern

1. 工厂方法模式可以解决简单工厂模式当中的OCP问题
    如何解决？一个具体产品对应一个具体工厂
    这样工厂类就不是一个全能上帝类了。
    同时也会符合OCP原则。

2. 工厂方法模式中的角色
    * 抽象产品角色
    * 具体产品角色
    * 抽象工厂角色
    * 具体工厂角色

3. 工厂方法模式的优点：
    * 当扩展一个产品的时候，符合OCP原则，因为只需要添加两个类，一个具体产品类，一个具体工厂类，并没有修改源代码
